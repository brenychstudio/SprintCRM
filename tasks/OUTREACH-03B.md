# OUTREACH-03B — Supervised AI Research Job

## Production acceptance closure record

- **Task goal:** record completed production acceptance for supervised AI research, perform a read-only language-metadata investigation without an OpenAI call, update the acceptance/roadmap documentation, and merge PR #20 after all required checks and green CI.
- **Current-state evidence:** branch `codex/outreach-03b-ai-research` is clean and tracks its origin. Production reports completed research job `d0a4f643-7407-42dd-b951-9a4ac16c3505` for request `6cd4d50f-cd3e-41aa-865a-c4e597596aab`, using `outreach_research_v2` / `research_v2` on `gpt-5.4-mini`; Version 1 and Version 2 are immutable and the observed workflow remains supervised.
- **Files inspected:** `AGENTS.md`, branch status, this task record, accepted-checkpoint conventions, current-state/architecture/ADR documentation, migration history, research helper/runtime code, client mappers, and the production ledger/snapshot shape through read-only queries that exclude narrative and lead data.
- **File plan:** update only `tasks/OUTREACH-03B.md`, `docs/current-state.md`, `docs/architecture/outreach-target.md`, `docs/decisions/0004-domain-restricted-ai-research.md`, and `docs/accepted-checkpoints/OUTREACH-03B.md` unless the investigation proves a future runtime language value is persisted or exposed incorrectly. No migration, deployment, secret, completed production row, or provider request is planned.
- **Implementation plan:** inspect only output-payload keys, language-metadata values, snapshot columns, and relevant mapper/resolver code; distinguish persisted metadata, diagnostic inference, mapper defaulting, and wrong-field reporting. If runtime is correct, document the diagnostic finding only. If future Draft generation could consume a wrong runtime value, make the smallest code-only resolver-based correction and add focused tests.
- **Risks and safeguards:** do not print generated narrative, website text, lead data, credentials, or provider prompts. Preserve immutable snapshots and ledger rows. Do not start OUTREACH-03C, call OpenAI, apply migrations, deploy a function, or send/approve/generate a message.
- **Acceptance criteria:** evidence records the completed job and all stated no-outbound safeguards; the language finding is evidenced without private content; documentation marks OUTREACH-03B ACCEPTED and defines OUTREACH-03C only as the next supervised-draft milestone; all required checks and linked read-only/dry-run migration checks pass; PR #20 is updated, green, ready, merged by the repository method, and local `main` equals `origin/main` with a clean worktree.
- **Test and manual-smoke plan:** run `verify:migrations`, typecheck, lint, unit tests, build, diff check, migration list, and dry-run. This documentation-only closure has no UI change; authenticated light/dark acceptance was completed in the supplied production evidence.
- **Proposed commit:** `docs(outreach): accept supervised AI research`.
- **Documentation impact:** retain the earlier wrapper/activity-owner/V2-start forward fixes and quality fix history, record non-blocking token/pre-staging debt, publish the language-diagnostic conclusion, and add the accepted OUTREACH-03B checkpoint.

## Pre-edit record

- **Task goal:** add one explicit, supervised AI research operation for a single Campaign Full Workspace member. It may create a versioned AI research snapshot from public website context, but must never generate messages, approve/send outreach, invoke Gmail, create follow-ups, batch work, retry providers, or run in the background.
- **Current-state evidence:** `main` is synchronized with `origin/main`; OUTREACH-03A is accepted. The existing `outreach-ai-runtime` Edge Function already provides authenticated, CORS-restricted, server-only Responses API runtime probes plus the `ai_generations` ledger and service-role RPC pattern. The next migration is `20260801000003`.
- **Files inspected:** `AGENTS.md`, Git status, OUTREACH-03A task/checkpoint evidence, `.env.example`, package scripts, feature flags/tests, the AI runtime Edge Function, and shared AI runtime helpers.
- **Files to inspect:** required campaign API/types/workflow/workspace/form, AI runtime card, i18n provider/locales, database types, foundation and AI migrations, research RPC migrations, validation scripts/tests, and the requested architecture/decision/current-state documents.
- **Data-flow plan:** user click → authenticated user-scoped context/website read → service-role `start_ai_research_job` → domain-restricted Responses API request with public campaign/lead fields only → strict result/source validation → service-role `finish_ai_research_job` → immutable `research_snapshots` version and canonical activity → invalidate/refetch workspace and summaries for human review.
- **Migration plan:** create additive `20260801000003_ai_research_job.sql`; add nullable campaign proof context; create/restrict idempotent start/finish RPCs; preserve organization RLS and browser read-only `ai_generations`; use atomic, locked version allocation; add audit and compatible canonical activity rows. Rollback is a server/client kill switch or an additive forward fix—historical ledger and snapshots will not be removed/re-written.
- **Function plan:** retain `runtime_probe` unchanged and route `generate_research` through pure shared request, URL, prompt, output/source, usage, and safe-error helpers. The function will use a user-scoped client for context reads and service role only for the two lifecycle RPCs; it will call Responses with `store:false`, a 45-second abort, no retry, and one domain-restricted `web_search` tool.
- **UI plan:** add the optional proof context field to campaign create/edit; add a feature-gated AI Research card adjacent to Manual research in the existing workspace; expose clear pending/completed/failed/website-required states and metadata; refresh existing research without touching messages or approvals.
- **Privacy boundaries:** no browser secret; never send contacts, email, phone, personal notes, IDs, messages, ownership, audit data, or credentials to OpenAI. Send only listed public lead and campaign context; web sources are constrained to validated public company domain. Human review remains mandatory.
- **Failure modes:** invalid/missing/private/placeholder URLs, authorization denial, disabled runtime/research flags, missing configuration, provider rate limit/timeout/error, malformed structured output, missing/unrelated evidence, conflicting/terminal ledger updates, and persistence failure return safe stable errors and never save a partial snapshot.
- **Acceptance criteria:** migration, function, UI and i18n support a single explicit research generation; results are strictly validated, idempotent, audited and immutable; later campaign statuses never regress; no outbound/message/send/Gmail behavior changes; flags default off; required checks and non-applying linked-migration verification complete.
- **Test plan:** unit coverage for flags, UUID/request/URL/prompt/result/source/usage/error helpers plus workspace state/invalidation/no-message side effects; migration verifier coverage; run migration/type/lint/test/build/diff checks and linked migration/dry-run commands. No provider calls are made in implementation testing.
- **Production rollout plan:** do not mark accepted until reviewed migration application, Edge Function deployment, `AI_RESEARCH_ENABLED` configuration, local client activation, an authenticated single real-site smoke in light/dark themes, provider usage/evidence inspection, ledger/snapshot/audit inspection, and confirmation of no message/send/status regression.
- **Proposed commit:** `feat(outreach): add supervised AI research job`
- **Documentation impact:** update this record, current state, outreach target architecture, and add a focused source-policy ADR if existing AI-boundary ADRs do not sufficiently capture domain-restricted research.

## Rollout and forward-fix

The migration is additive. Keep `AI_RESEARCH_ENABLED=false` and `VITE_AI_RESEARCH_ENABLED=false` until reviewed rollout. For a production issue, disable either kill switch and deploy an additive SQL/function/client forward-fix. Never delete/rewrite completed ledger rows or immutable snapshots.

## Production campaign wrapper forward-fix

- A production campaign-creation smoke confirmed PostgreSQL error `22P02`: `invalid input syntax for type uuid: "(<campaign composite row>)"`.
- Root cause: the eight- and ten-argument proof-context wrappers in the applied `20260801000003_ai_research_job.sql` used scalar `select public.create_manual_campaign(...) into v_campaign` and `select public.update_manual_campaign(...) into v_campaign`. The legacy RPCs return `public.campaigns` composite rows, so PL/pgSQL attempted to coerce the composite row text into `v_campaign.id`.
- Forward fix: `20260801000004_fix_campaign_proof_context_wrapper_assignment.sql` recreates the same authenticated-only, `security invoker` wrappers with `select * into v_campaign from public.…`, retaining legacy organization/RLS behavior and nullable `nullif(btrim(p_proof_context), '')` updates.
- Campaign creation retest is pending reviewed migration application. No OpenAI call occurred during the failing smoke or this forward-fix work.

## Production AI research activity-owner forward-fix

- **Task goal:** correct the completed AI research persistence boundary without modifying applied migrations, then provide a service-only path to fail an orphaned stale pending research job after deployment.
- **Current-state evidence:** a production research smoke returned the exact PostgreSQL error `null value in column "owner" of relation "activities" violates not-null constraint`. `start_ai_research_job` returned 200, OpenAI completed its request, and `finish_ai_research_job` reached its completed-result path before PostgREST returned 400 and the Edge Function returned `persistence_error`. PostgreSQL rolled back the complete transaction, so no snapshot, completed audit event, provider/usage metadata, CRM message/send, or campaign-member status mutation was committed. Token usage was therefore not recorded. The ledger contains one orphaned pending research job requiring recovery after deployment.
- **Files inspected:** `AGENTS.md`, Git branch/status, the two applied OUTREACH-03B migrations, `ai_generations`/`activities` schema and foundation migrations, the existing finish RPC and Edge Function caller, generated database types, migration verifier, package scripts, and this task record.
- **File plan:** add `20260801000005_fix_ai_research_activity_owner.sql`; update `scripts/verify-migrations.mjs`, `src/lib/supabase/database.types.ts`, and this task record only.
- **Implementation plan:** recreate only `finish_ai_research_job` at its existing signature and protections; validate the stored initiating owner before the completed transition; pass `v_job.owner` explicitly to the canonical `research_saved` activity; add service-role-only `fail_stale_ai_research_job` for research rows pending at least five minutes; retain snapshots, member status, activity ownership constraints, audit append-only behavior, and no browser execution.
- **Risks and forward-fix:** failed recovery attempts and terminal jobs are rejected without rewriting them. The recovery RPC accepts only the controlled `persistence_recovery` code, bounds duration calculation safely, writes no snapshot, and changes no campaign-member status. Rollback is an additive forward-fix or runtime kill switch; historical ledger, snapshot, and audit records are never deleted or rewritten.
- **Acceptance criteria:** the new migration follows `20260801000004`; its canonical research activity explicitly lists `owner` and uses validated `v_job.owner`; finish and recovery RPCs remain `security definer`, service-role-only, organization membership-validated, and unavailable to browser roles; verifier/type/test/build/linked dry-run checks pass; no migration, deployment, OpenAI call, secret change, or production recovery is performed in this task.
- **Test and manual-smoke plan:** run the required static and linked read-only/dry-run commands. After reviewed deployment, recover the orphaned pending job with the service role, then run an authenticated real-site research smoke in both themes and inspect ledger, snapshot, activity, audit, provider metadata, and absence of message/send/status regression. Smoke retest is pending.
- **Proposed commit:** `fix(outreach): persist AI research activity owner`.
- **Documentation impact:** this task record now preserves the production failure, confirmed OpenAI call, rollback effects, recovery requirement, new migration/RPC, and pending smoke retest.

## Activity-owner forward-fix implementation record

- Added `20260801000005_fix_ai_research_activity_owner.sql`. It recreates only the existing finish RPC signature, fails completed research safely if the ledger owner is unexpectedly null, and writes the canonical `research_saved` activity with explicit `owner = v_job.owner`.
- Added service-role-only `fail_stale_ai_research_job(uuid, uuid, text)`. It validates organization membership, the `research` job type, pending state, five-minute staleness, and the controlled `persistence_recovery` code; it safely marks the ledger failed and appends a system `ai.research.failed` audit event without creating a snapshot or changing campaign-member status.
- Synchronized the generated database type declaration and expanded the migration verifier for ordering, explicit activity ownership, service-role-only grants, browser-execution denial, stale-recovery guards, and the absence of the broken ownerless activity insert.
- Completed: `npm run verify:migrations`, `npm run typecheck`, `npm run lint`, `npm run test` (49 tests), `npm run build`, and `git diff --check`. The build has the existing Vite large-chunk warning only.
- Linked `npx supabase migration list --password $env:SUPABASE_DB_PASSWORD` and `npx supabase db push --dry-run --password $env:SUPABASE_DB_PASSWORD` were attempted without applying anything, but both are currently blocked by PostgreSQL password authentication failure (`28P01`). No secret was changed. Linked migration verification and dry-run must be rerun with the correct existing database password before deployment.

## Implementation record

- Added `20260801000003_ai_research_job.sql` with nullable campaign proof context, compatible campaign RPC overloads, service-role-only idempotent research start/finish RPCs, terminal-state guards, immutable AI snapshots, canonical CRM activity/audit records, browser ledger read preservation, and browser write revocation.
- Extended the existing runtime Edge Function with `generate_research` while retaining `runtime_probe`; all CRM context reads are user-scoped, service role is limited to lifecycle RPCs, OpenAI receives only the allowed public fields, and no provider call is made by tests.
- Added pure URL, request, prompt, strict result/source, usage, and safe-error helpers; domain-restricted OpenAI web search is configured with `store:false`, a 45-second abort, bounded output, and no retry/background mode.
- Added default-off client/server research gates, proof-context campaign form/API/types support, generated type synchronization, four-locale workspace strings, and a feature-gated AI Research card that invalidates workspace/overview/lead/job queries and never invokes message or approval mutations.
- Completed local static verification: `npm run verify:migrations`, `npm run typecheck`, `npm run lint`, `npm run test:unit`, `npm run build`, and `git diff --check` (build retains the known Vite large-bundle warning).
- Linked read-only verification after the production smoke confirms remote migrations end at `20260801000003`; `npx supabase migration list --password $env:SUPABASE_DB_PASSWORD` identifies only `20260801000004` as pending, and `npx supabase db push --dry-run --password $env:SUPABASE_DB_PASSWORD` would push only the campaign proof-context wrapper forward-fix. Nothing was applied or deployed.

## Historical rollout status

This historical status is superseded by the production acceptance closure record at the end of this task file.

## OUTREACH-03B-QUALITY-02 — Research parser diagnostics and proof-title matching

### Pre-edit record

- **Task goal:** make the rejected production research response diagnosable without exposing provider content, correct proof-title extraction for the real Oria House Barcelona structural pattern, preserve normalized provider usage and provider IDs on semantic rejection, and return research-specific safe server wording. The runtime-probe contract must remain unchanged.
- **Current-state evidence:** production request `fe5fb30f-cdfc-40a7-9e07-2d9a24006477` called OpenAI and recorded provider IDs, but terminally failed with no snapshot. `parseResearchResult(...)` returned `null`, hiding the semantic reason. Its current caller normalizes usage only after parsing, while `finishResearchFailure` passes null token values. The applied `finish_ai_research_job` signature already accepts provider IDs plus input, cached-input, output, total token, and duration fields; no migration is needed. `extractProofContextIdentifiers` applies a title-case expression to the complete proof context using `\s`, allowing a candidate to cross a newline.
- **Files inspected:** `AGENTS.md`, git branch/status/history, `tasks/OUTREACH-03B.md`, `supabase/functions/_shared/ai-research.ts`, its unit tests, `supabase/functions/_shared/ai-runtime.ts`, the `outreach-ai-runtime` Edge Function, applied research migrations, generated RPC types, package scripts, and draft PR #20.
- **File plan:** update only the shared research helper and tests, the existing Edge Function, and this task record. No migrations, generated database types, UI, secrets, completed jobs, snapshots, or production data will be changed.
- **Implementation plan:** return a discriminated safe parse result with stable reason codes while retaining the nullable wrapper; extract proof identifiers per non-empty line with horizontal-space-only title matching; normalize provider usage immediately after JSON decoding; pass it and provider IDs through rejected-result finalization with `research_validation:<reason>` only; and add a research-specific safe error response path.
- **Risks and safeguards:** preserve existing quality thresholds and source validation; do not log or persist raw provider response, prompt, narrative, CRM context, or secrets in diagnostics. Semantic rejection must finalize only the pending ledger row and must not create a snapshot, message, approval, Gmail/send operation, campaign mutation, or status change. Rollback is the existing server/client kill switch or a forward code fix; no terminal data is rewritten.
- **Acceptance criteria:** Oria House Barcelona is extracted exactly once from the real two-line proof shape and a Spanish matching recommendation passes; unrelated cases return `proof_context_mismatch`; each parser branch has a safe reason; valid `research_v2` remains valid; rejected results store IDs and normalized usage, no raw content, and research wording never says probe; runtime-probe wording remains unchanged.
- **Test and manual-smoke plan:** add focused unit coverage for parser reasons/proof matching and failure-persistence payload boundaries; run `npm run verify:migrations`, `npm run typecheck`, `npm run lint`, `npm run test:unit`, `npm run build`, and `git diff --check`. No provider call, migration application, deployment, or authenticated UI smoke occurs locally. After reviewed Edge Function redeployment, run an authenticated Version 2 research retest in light and dark themes and inspect the terminal ledger, usage, IDs, absence of a snapshot on rejection, audit/activity state, and no message/send/status mutation.
- **Proposed commit:** `fix(outreach): diagnose research validation failures`.
- **Documentation impact:** record this failure, its post-parse usage-loss root cause, hidden parser reason, newline-crossing proof-title defect, and pending deployment/retest here; no current-state claim changes before deployment.

### Implementation record

- Request `fe5fb30f-cdfc-40a7-9e07-2d9a24006477` is terminal and consistent: OpenAI was called, no cleanup is required, and no research snapshot was created. Its usage was lost only because the former code normalized usage after semantic parsing; the parser also reduced every rejection to `null`, hiding the precise safe reason.
- `parseResearchResultDetailed` now returns a discriminated result with stable safe reasons; `parseResearchResult` remains the compatibility wrapper. The Edge Function normalizes usage immediately after provider JSON decoding and, on semantic rejection, records provider IDs, normalized usage, duration, null output payload, `invalid_provider_response`, and only `research_validation:<reason>`.
- Proof identifiers are now extracted from non-empty lines independently, use horizontal spaces for title-like matching, normalize/deduplicate candidates, retain heading/link/label support, and reject long sentence-like candidates. The actual `Oria House Barcelona` title-plus-paragraph shape now yields exactly the usable title, allowing a grounded Spanish recommendation.
- `generate_research` now uses a research-specific safe error responder. The provider-validation message says the research result did not pass validation; neither the internal reason nor raw provider output/prompt/CRM data is returned, persisted, or logged. Runtime-probe wording is unchanged.
- Completed locally without a provider call, migration, production mutation, or secret change: `npm run verify:migrations`, `npm run typecheck`, `npm run lint`, `npm run test:unit` (59 tests), `npm run build`, and `git diff --check`. The build has the known large-chunk warning only.
- **Rollout hold:** Edge Function redeployment and authenticated Version 2 retest in light and dark themes remain pending. Confirm rejected-result ledger usage/IDs and absence of snapshot, message, approval, Gmail, send, campaign-status, and other workflow mutations after deployment.

## OUTREACH-03B-QUALITY-01 — AI Research Quality and Reviewability

- **Task goal:** improve the next supervised AI research version’s language fidelity, proof-context grounding, confidence calibration, constrained-domain token use, and evidence reviewability. Historical research version 1 and completed production ledger/snapshot rows are immutable and out of scope.
- **Current-state evidence:** the first completed production research for Hotel Marina Badalona was technically successful (`gpt-5.4-mini`, 9,165 input / 438 output / 9,603 total tokens, 4,064 ms, confidence 0.92, five valid unique sources) but quality-rejected: Spanish lead/campaign context yielded English output, non-empty Oria House Barcelona proof context yielded `recommended_case = null` and a contradictory no-proof warning, confidence exceeded supportable website-only bounds, and the UI truncated otherwise valid evidence.
- **Files inspected:** `AGENTS.md`, current branch/status, research shared helpers/tests, Edge Function request boundary, AI Research card, workspace evidence editor, campaign API/types, locale dictionaries, and the requested current-state/architecture/decision documents.
- **File plan:** update only the shared research helpers/tests, Edge Function request configuration, AI research/workspace UI and tests, four locale dictionaries, campaign research client types, and the requested documentation. No migration, generated database types, secrets, production rows, or provider calls are planned.
- **Implementation plan:** split trusted Responses instructions from serialized untrusted public CRM input; resolve `en`/`es`/`uk`/`ru` output language with English fallback; validate proof-context semantics and confidence/evidence bounds server-side before persistence; retain domain restriction, low search context, `store:false`, no retry, and exact usage recording while adding `max_tool_calls: 2` and a 900-token output cap; replace evidence rows with responsive review cards and correct metadata/contact fallback labels.
- **Risks and rollback:** malformed or contradictory provider results must fail the job without a snapshot. A rollout issue is handled by existing kill switches or an additive server/client forward fix; do not rewrite or delete historical jobs/snapshots. The v2 schema/prompt names make the revised contract auditable.
- **Acceptance criteria:** next successful generation is an immutable version 2; output is in resolved language; verified proof context is used without invented claims; website-only confidence is at most 0.85; evidence is two to three unique, concise, reviewable sources; no messages, approvals, sends, status changes, direct website fetches, provider retry, or automatic behavior are introduced.
- **Test/manual-smoke plan:** cover resolver, trusted/untrusted prompt separation and injection resistance, proof semantics, confidence/evidence limits, provider request limits/privacy, evidence-card rendering/layout, metadata/version/contact fallback, and no-message/approval/sending behavior. Run required static gates and manually inspect authenticated light/dark workspace states without calling a provider.
- **Proposed commit:** `fix(outreach): improve AI research quality controls`
- **Documentation impact:** record this production result as technically successful but quality-rejected in this task record, current state, target architecture, and domain-restricted research decision. OUTREACH-03B remains unaccepted.

### OUTREACH-03B-QUALITY-01 implementation evidence

- Implemented `research_v2` / `outreach_research_v2` without a migration. The existing finish RPC continues to allocate the next immutable `research_snapshots.version`; no historical `ai_generations` or research version 1 row was edited.
- The provider request now uses top-level trusted instructions and a distinct serialized public input, resolves `en`/`es`/`uk`/`ru` language server-side, constrains output to two or three evidence items, `max_tool_calls: 2`, `max_output_tokens: 900`, `search_context_size: low`, `store: false`, and the existing validated-domain-only web search with no retry/direct fetch.
- Provider output is rejected before persistence for confidence over 0.85, duplicate normalized URLs, evidence note/count violations, missing/mismatched proof cases, contradictory no-proof warnings, or incorrect empty-proof case/warning semantics. Exact usage accounting and supervised approval/send boundaries are unchanged.
- Completed: `npm run verify:migrations`, `npm run typecheck`, `npm run lint`, `npm run test` (53 tests), `npm run build`, and `git diff --check`. The build retains only the existing Vite large-chunk warning. No provider smoke was run.
- Authenticated light/dark browser smoke could not be performed in this environment because no controlled browser session is available. The component tests cover responsive/dark class support, full notes/links, removal, numeric version display, fallback contact wording, and the unchanged no-message/no-approval behavior; authenticated theme smoke remains a release check.

## Production AI research V2 start-contract forward-fix

- **Task goal:** add a forward-only database fix that lets the deployed supervised research Edge Function start Version 2 jobs while retaining Version 1 compatibility. The start boundary remains ledger-only: it creates neither a CRM activity nor a research snapshot and does not mutate campaign-member status.
- **Current-state evidence:** production request `287e254e-d9da-4c00-8dc9-8cb9f7975c70` failed at PostgREST `start_ai_research_job` with HTTP 400 / PostgreSQL SQLSTATE `22023`, `Invalid research request`. The failure occurred before OpenAI: `public.ai_generations` has no row, `public.audit_events` has no row, no research snapshot exists, and no provider token usage was incurred. The deployed Edge Function sends `p_prompt_version = outreach_research_v2` for `research_v2`; the deployed start RPC accepts only `outreach_research_v1` and hardcodes `outreach_research_v1` / `research_v1`. The V2-compatible finish RPC was never reached.
- **Files inspected:** `AGENTS.md`, Git branch/status, the original research-job migration, the activity-owner forward-fix, deployed Edge Function caller/shared V2 version constants, generated database types, migration verifier, package scripts, and this task record.
- **File plan:** add only `supabase/migrations/20260801000006_fix_ai_research_v2_start_contract.sql`; update `scripts/verify-migrations.mjs` with static start-contract assertions; update this task record. Generated types need no change because the function signature and return shape are preserved.
- **Implementation plan:** recreate only `public.start_ai_research_job` with its existing signature, return table, `security definer` mode, `public` search path, role/organization/membership/campaign-member checks, request-id idempotency, ledger/audit behavior, grants, and browser execution revocation. Accept only V1/V2 prompts, calculate the matching schema version, and persist the supplied valid prompt.
- **Risks and forward-fix:** invalid, missing, or unknown prompt versions must retain the safe `22023` invalid-request error; a rollout regression is handled by a later additive migration or the existing research kill switch. Historical ledger, audit, and immutable snapshot rows are never edited or deleted.
- **Acceptance criteria:** migration `20260801000006` follows `20260801000005`; V1 maps to `research_v1`, V2 maps to `research_v2`, unknown prompts are rejected before a ledger/audit insert, and the service-role-only, idempotent supervised boundary has no OpenAI/provider, message, approval, send, Gmail, activity, or member-status behavior.
- **Test and manual-smoke plan:** extend the migration verifier for ordering, both allowed prompts and mappings, non-hardcoded schema mapping, and execute grants/revocations; run all required static gates plus linked migration list and dry-run without applying changes. An authenticated Version 2 retest remains pending reviewed migration application; no provider call is made by this work.
- **Proposed commit:** `fix(outreach): support AI research v2 job start`.
- **Documentation impact:** this record preserves the exact production SQLSTATE `22023` failure, its pre-OpenAI/no-token/no-ledger outcome, the Edge Function/RPC V2 mismatch, migration `20260801000006`, and the pending authenticated Version 2 retest.

### Implementation and verification evidence

- Added `20260801000006_fix_ai_research_v2_start_contract.sql`. It recreates only `start_ai_research_job` and retains the deployed five-argument signature, return shape, `security definer` / `search_path`, request-id lookup, organization-scoped member/lead validation, requested audit event, and service-role grant/browser revocation. It accepts exactly `outreach_research_v1` and `outreach_research_v2`, persisting the supplied prompt and its matching `research_v1` / `research_v2` schema version.
- Expanded the static migration verifier for ordering after `20260801000005`, both prompt mappings, null/unknown safe invalid-request rejection, no hardcoded schema version, no start-time activity/member mutation or provider/outbound behavior, and service-only execution.
- Completed: `npm run verify:migrations` (17 migrations), `npm run typecheck`, `npm run lint`, `npm run test:unit` (53 tests), `npm run build`, and `git diff --check`. The build retains only the existing Vite large-chunk warning.
- Both linked non-applying commands were attempted with the existing `SUPABASE_DB_PASSWORD`: `npx supabase migration list --password $env:SUPABASE_DB_PASSWORD` and `npx supabase db push --dry-run --password $env:SUPABASE_DB_PASSWORD`. Both were blocked before migration inspection by PostgreSQL authentication SQLSTATE `28P01`; no secret, migration, Edge Function, provider call, or production data changed. The expected state—only `20260801000006_fix_ai_research_v2_start_contract.sql` pending—must be confirmed after the existing credential is corrected.
- Authenticated Version 2 production retest remains pending reviewed migration application. No authenticated browser session is available here; this backend-only migration has no light/dark UI change.

## Production acceptance closure

**Status: ACCEPTED.**

- Reviewed production rollout applied the campaign-wrapper, activity-owner, and V2 start-contract forward fixes; the deployed Edge Function then completed the successful V2 research job without an OpenAI request during this closure task.
- Completed job: `d0a4f643-7407-42dd-b951-9a4ac16c3505`; request: `6cd4d50f-cd3e-41aa-865a-c4e597596aab`; status: `completed`; prompt/schema: `outreach_research_v2` / `research_v2`; provider/model: `openai` / `gpt-5.4-mini`.
- Usage and quality: 9,309 input, 0 cached-input, 438 output, and 9,747 total tokens in 4,669 ms; confidence 0.78; three distinct reviewable evidence sources; verified Oria House Barcelona proof context; Spanish narrative; strict schema and semantic validation passed; human review remains required.
- State and safety: immutable Version 1 remains and immutable Version 2 exists; exactly two versions are saved; Version 2 links to the completed ledger; requested/completed audit events exist; the canonical `research_saved` activity has an owner; the member is `research_ready`; no message version, approval, Gmail draft/send, outbound send, campaign-status regression, or pending/failed job for the successful request exists.
- Language-metadata investigation was read-only and did not expose narrative or lead data. The completed payload has only `confidence`, `evidence`, `observed_opportunity`, `recommended_case`, `recommended_offer`, and `warnings`; no `language`, `output_language`, or `locale` key exists. `research_snapshots` has no language column. The server resolves language from lead, then campaign default, before the provider call; no client/server mapper persists or exposes a research-language value. The reported `en` was therefore a diagnostic/report fallback from an absent field, not an incorrect production value or a future Draft-generation runtime dependency. No runtime code or production row changed.
- Closure verification passed: `npm run verify:migrations` (17 migration files), `npm run typecheck`, `npm run lint`, `npm run test:unit` (59 tests), `npm run build` (known Vite large-bundle warning only), and `git diff --check`. Linked `npx supabase migration list --password $env:SUPABASE_DB_PASSWORD` matched local and remote through `20260801000006`; linked `npx supabase db push --dry-run --password $env:SUPABASE_DB_PASSWORD` reported the remote database up to date with no migration, seed, or role change.
- Non-blocking debt: token reduction was not achieved (Version 1: 9,603 total tokens; Version 2: 9,747); cost estimation remains unconfigured; Docker/local Supabase reset, behavioral RLS integration tests, and the existing Vite large-bundle warning remain pre-staging debt.
- Next milestone only: **OUTREACH-03C — Supervised AI Draft Generation**. It may generate an explicit single-click message draft only from campaign context, the reviewed latest research snapshot, verified proof context, lead/campaign language, campaign tone, and offer. It must use immutable message versions and human review, with no Gmail send, automatic approval, or automatic campaign progression.
