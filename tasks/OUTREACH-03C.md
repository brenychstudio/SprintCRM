# OUTREACH-03C — Supervised AI Draft Generation

## Pre-edit record

- **Task goal:** Add explicit, human-initiated AI email-draft generation to the existing Campaign Full Workspace. A confirmed latest research snapshot is the only research input; successful generation saves an immutable `outbound_messages` draft version and moves a member no further than `draft_ready`.
- **Current-state evidence:** Clean `main` and `origin/main` were synchronized at `d52d983` (`docs(outreach): accept supervised AI research`) before branch creation. The accepted OUTREACH-03B checkpoint, AI research edge-function operation, `ai_generations` ledger, `research_snapshots`, immutable `outbound_messages`, and migrations through `20260801000006` are present.
- **Files inspected:** `AGENTS.md`, git state/history, migration inventory, `docs/accepted-checkpoints/OUTREACH-03B.md`, current AI runtime/research references, campaign API/types/workspace references, and schema references.
- **File plan:** Extend the AI runtime and shared draft-safe parsing helpers; add one additive migration and generated database types; integrate a feature-gated workspace card/API/types/tests; add localizations; update migration verification and the listed architecture/current-state/decision documents; add a focused ADR if needed.
- **Implementation plan:** Follow the accepted research-job pattern with server-only start/finish RPCs. The function authorizes the caller, derives minimal data itself, calls Responses once with `store:false` and strict `draft_v1` output, normalizes usage before validating, then atomically persists or safely fails the ledger. The workspace requires a version-specific research confirmation and refreshes persisted immutable drafts.
- **Security boundaries:** Browser sends only operation/member/confirmed-snapshot/request IDs. The server derives all CRM content after actor and organization checks. No browser OpenAI key, provider tools, web search, arbitrary fetch, email/phone/notes/IDs/audit/prior drafts in provider input, raw provider output/prompt/secrets persisted, automatic retry, sending, approval, Gmail action, or autonomous state progression.
- **Lifecycle/state transitions:** Start allows only `research_ready` and `draft_ready`, creates a pending `job_type=draft` ledger and human audit event without message/activity/status mutation. Success creates one immutable `source=ai,status=draft` version, completes the ledger, writes AI audit plus canonical `outreach_draft_saved`, and keeps/moves the member at `draft_ready`. Failure completes only the pending ledger as failed with safe data. Changed research fails as `stale_research` and creates no message.
- **Risks:** Existing production accepted patterns and generated database types can drift; SQL function signatures and activity owner conventions require exact matching; strict semantic validation must avoid leaking internal reasons; UI query cache changes must retain existing manual review behavior. Forward-fix/rollback is an additive migration and function replacement only; no destructive migration is planned.
- **Acceptance criteria:** All requested flags default disabled; strict authorized `generate_draft` operation; provenance and immutable message versioning; no progression beyond `draft_ready`; localized, responsive confirmation UI; safe failures/usage; all specified unit and repository verification checks pass.
- **Tests:** Add focused feature-flag, runtime request/privacy/language/structured-output/error tests, plus workspace confirmation and draft integration tests where the existing test structure supports them. Run migration verifier, typecheck, lint, unit tests, build, and `git diff --check`.
- **Migration/deployment plan:** Add the next timestamped additive migration after `20260801000006`; update schema/types/verifier; run `supabase migration list` and `db push --dry-run` only. Do not apply a migration, deploy the function, change secrets, or call OpenAI. Production enablement requires reviewed apply, `AI_DRAFT_GENERATION_ENABLED=true`, local `VITE_AI_DRAFT_GENERATION_ENABLED=true`, deployment, and supervised smoke.
- **Production smoke plan:** Authenticated Hotel Marina Badalona Spanish draft with reviewed latest research: verify Version 1 provenance and output, manual edit creates Version 2, no automatic review/approval/Gmail/send, inspect ledger/audits/activity and OpenAI usage, then repeat light/dark/narrow workspace checks.
- **Proposed commit:** `feat(outreach): add supervised AI draft generation`
- **Documentation impact:** Update `docs/current-state.md`, `docs/architecture/outreach-target.md`, `docs/decisions/0002-controlled-ai-boundary.md`, plus a focused supervised-draft ADR if the existing decision does not fully cover confirmation, provenance, stale protection, and no-send guarantees. This task file remains unaccepted until externally reviewed.

## Implementation record

- Added `20260801000007_supervised_ai_draft_generation.sql`: service-role-only idempotent start/finish/recovery RPCs, latest-research confirmation and stale protection, immutable AI message versions, ledger/audit/activity provenance, and `draft_ready`-only progression.
- Extended `outreach-ai-runtime` with a foreground `generate_draft` operation and `draft_v1` strict parser/request builder. Provider input excludes private contact channels, notes, IDs, prior drafts, audit/activity data, and credentials; it uses `store:false`, no tools, no web search, no retry, and a 25-second abort timeout.
- Added the feature-gated Campaign Full Workspace AI Draft card, persisted-result query invalidation, confirmation control, compact metadata, localized strings, and manual-message refresh through the existing workspace query.
- Added focused parser/privacy/language/UI/feature-flag tests and extended migration verification. Generated database RPC types were synchronized manually because this task does not apply the migration.

## Verification record

- Passed: `npm run verify:migrations` (18 migrations), `npm run typecheck`, `npm run lint`, full `npm run test:unit` (68 tests), `npm run build`, and `git diff --check` in the final verification run.
- Linked `npx supabase migration list --password $env:SUPABASE_DB_PASSWORD` and `npx supabase db push --dry-run --password $env:SUPABASE_DB_PASSWORD` were attempted without applying anything, but both were blocked by `SUPABASE_DB_PASSWORD` authentication failure (`SQLSTATE 28P01`). `deno check` could not run because Deno is not installed in this environment.
- Authenticated/light/dark/narrow production smoke is intentionally pending reviewed migration apply, feature enablement, Edge Function deployment, and an authorized smoke environment. No OpenAI call was made.

## OUTREACH-03C-FIX-01 draft body validation diagnostics and targeted correction

- **Task goal:** Make rejected draft bodies precisely diagnosable through safe internal validation reasons, preserve the generic browser error, and correct only deterministic false positives in the plain-text and multilingual CTA/language checks.
- **Current-state evidence:** The first authenticated Hotel Marina Badalona draft reached OpenAI and the persistence lifecycle completed correctly. OpenAI/provider IDs, normalized usage, and duration were retained; the terminal failed ledger has `completed_at` and the safe detail `draft_validation:invalid_body`, with 956 total tokens consumed. It created no outbound message version or CRM activity; the member remains `research_ready`; no review, approval, Gmail, send, campaign progression, stale-job recovery, or cleanup occurred. The grouped `invalid_body` detail did not identify the failed predicate.
- **Files inspected and file plan:** `AGENTS.md`, the current branch/status, `supabase/functions/_shared/ai-draft.ts`, `ai-draft.test.ts`, `outreach-ai-runtime/index.ts`, this task record, and `docs/current-state.md`. Update only the shared parser, its unit tests, runtime rejected-result mapping, and these records; add no migration and do not alter generated types.
- **Implementation plan and safeguards:** Split body validation into stable safe reasons, retain `invalid_provider_response` with the generic browser message, and persist only `draft_validation:<reason>`. Keep unsupported-claim, placeholder, invented-recipient/sender, proof-context, human approval, no-send, and immutable-version protections intact. Provider bodies, prompts, CRM context, credentials, and raw provider responses remain neither persisted nor logged.
- **Narrow correction:** The prior CTA matcher accepted any question mark and its Spanish/Cyrillic markers were brittle. A Spanish body with a verified English company/case name containing `the` could therefore be classified as English when its accented Spanish CTA was not recognized. The parser now accepts normal multi-paragraph plain text and language-specific, low-pressure question structures in English, Spanish, Ukrainian, and Russian; it does not relax the CTA, language, format, claim, length, or pressure checks.
- **Risks, acceptance, and tests:** Main risks are weakening content safeguards or leaking generated content. Acceptance requires reason-specific failed validation, generic browser response, retained failed usage/provider IDs/duration with `output_payload=null`, and no message/activity/review/approval/Gmail/send/member mutation. Focused tests cover every body reason, valid multilingual CTAs, the Spanish false positive, strict `draft_v1`, and safe rejected-ledger fields. Required repository checks and `git diff --check` will be run before handoff.
- **Manual smoke, commit, and documentation:** No UI surface changed, so theme smoke is not applicable. Edge Function redeployment and one authenticated draft retest are pending; that retest must confirm the exact safe ledger detail and the same no-side-effect boundary. Proposed commit: `fix(outreach): diagnose AI draft body validation`. This task record and `docs/current-state.md` record the production observation and pending retest.

## OUTREACH-03C-FIX-02 deterministic missing-CTA completion

- **Task goal:** Make draft generation reliable when, and only when, the fully decoded provider result otherwise passes normal validation but lacks the required low-pressure CTA. Append one canonical CTA in the already resolved output language without a second provider request, then re-run the complete validator before persistence.
- **Current-state evidence:** The second authenticated Hotel Marina Badalona attempt called OpenAI successfully and consumed 709 input plus 285 output tokens (994 total). Provider IDs were retained, while the ledger finalized failed with `draft_validation:body_missing_low_pressure_cta`. It created no outbound message or CRM activity; the member remains `research_ready`; no review, approval, Gmail, send, campaign progression, cleanup, or historical-job change occurred. The validator correctly rejected the response.
- **Files inspected and file plan:** `AGENTS.md`, branch/status and PR #21, `supabase/functions/_shared/ai-draft.ts` and tests, `supabase/functions/outreach-ai-runtime/index.ts`, `20260801000007_supervised_ai_draft_generation.sql`, this task record, and `docs/current-state.md`. Update only the shared validation/completion helper and its tests, the Edge Function call site, and these records. No migration, generated type, secret, or historical production row changes are planned.
- **Implementation plan and safeguards:** First run normal complete validation. Only its exact `body_missing_low_pressure_cta` result may invoke in-memory completion, which appends a final separate paragraph containing exactly one language-specific canonical CTA. Re-run the complete validator over the completed body and persist only its validated subject/body/warnings plus safe `cta_completion: deterministic` metadata. All other reasons, including warning/proof failures exposed by the second pass, remain ordinary safe rejection with `output_payload=null`. There is one Responses request and no retry, truncation, raw output persistence, automatic send, or workflow advance beyond `draft_ready`.
- **Risks, acceptance, and tests:** Risks are a duplicate CTA, accidental correction of a different semantic defect, length overflow, metadata content leakage, or lifecycle side effects. Acceptance requires Spanish, English, Ukrainian, and Russian canonical completion; unchanged already-valid bodies; no correction for aggressive/multiple CTA, wrong language, claims, placeholders, or proof mismatch; a max-length failure without truncation; and preservation of the one-provider-call/no-retry, immutable `source=ai,status=draft`, research/ledger linkage, `outreach_draft_saved` activity, and draft-ready-only lifecycle. Run migration verification, typecheck, lint, unit tests, build, and `git diff --check`.
- **Manual smoke, commit, and documentation:** No UI surface changes, so theme smoke is not applicable. No provider call occurs during implementation. Edge Function redeployment and one authenticated retest remain pending after review. Proposed commit: `fix(outreach): complete missing AI draft CTA safely`. This task record and `docs/current-state.md` document the 956-token grouped first failure, FIX-01 diagnostics, the 994-token exact second failure, this narrow no-retry completion, and the pending deployment/retest.

## OUTREACH-03C-FIX-03 attempt-state and safe client error observability

- **Task goal:** Make the AI Draft card distinguish the current browser mutation from immutable historical ledger state, retain one client request ID per explicit attempt, and display only safe Edge Function failure diagnostics so the next supervised production test is observable without mistaking historical failures for a fresh provider call.
- **Current-state evidence:** After `outreach-ai-runtime` Version 9 deployed at `2026-08-08T11:01:22Z`, read-only production inspection found zero new draft `ai_generations` rows, requested/completed/failed draft audit events, and outbound messages. The red browser Failed state instead reflects the immutable preceding job created `2026-08-02T07:43:26.733741+00:00` (`request_id e1eb420a-241c-46a4-a471-0730a05e8abc`, `draft_validation:body_missing_low_pressure_cta`, 994 tokens). `AiDraftCard.tsx` currently treats `latestJob.generation_status === 'failed'` as a current failure after refresh, and `generateAiDraft` replaces every function error with a generic `Error`, discarding the safe `code`, `message`, `request_id`, and `retryable` envelope.
- **Files inspected and file plan:** `AGENTS.md`, current branch/status, `AiDraftCard.tsx` and tests, campaigns API/types, all four campaign AI-draft locale entries, installed `@supabase/functions-js` v2.97 invoke/error types, this task record, and `docs/current-state.md`. Update only the card, campaigns API/types, focused unit tests, localization strings, and documentation. No migration, generated database type, Edge Function, secret, provider call, deployment, or historical production row will change.
- **Implementation plan:** Export a typed `AiDraftGenerationError` and parse only the known safe error envelope from the installed `FunctionsHttpError.context` `Response` API. Generate `crypto.randomUUID()` once at explicit click time, retain it in local mutation-attempt state, and pass that same ID into the mutation. While a current attempt is pending, successful, or failed, it controls the displayed status across query invalidations; a matching persisted completed job may restore Completed after refresh. A persisted failed job appears only as compact neutral previous-attempt history.
- **Risks and safeguards:** Query refetches can race the mutation and stale data can obscure attempts; local state is therefore authoritative until the mutation terminal state, with persisted completion used only when it matches latest message/research. UI renders only generic failure copy, allowlisted public error codes, and the first eight characters of the local request ID. It never renders internal validation details, raw Edge Function/provider bodies, headers, prompt, lead data, secrets, provider IDs, or service-role information. No automatic retry, send, approval, workflow progression, or changed enablement rule is introduced.
- **Acceptance criteria and tests:** Historical failed jobs never produce main Status Failed or disable a confirmed usable Generate action. Current pending, success, and failure states win over query refresh; failures—including pre-ledger failures—show the safe code and compact local attempt ID; matching completed persisted jobs restore Completed; a new click uses one new ID; invalid-provider responses never expose validator reasons; duplicate clicks stay disabled; no provider retry occurs. Tests cover the specified fresh-load, success, failure, race, identifier, and responsive/theme-compatible card cases, plus safe client error parsing.
- **Manual smoke, commit, and documentation:** After frontend restart, authenticate and verify light, dark, and narrow card presentation with a historical failed row, a current safe failure, and a matching completed job. No production generation occurs in this implementation. Required checks are migration verification, typecheck, lint, unit tests, build, and diff check. Proposed commit: `fix(outreach): distinguish AI draft attempt state`. Edge Function deployment is unnecessary unless server code changes (none planned); an authenticated production generation remains pending after frontend redeployment, and FIX-02 has not yet been production-tested after Version 9.

### FIX-03 implementation and verification record

- The card now holds a single explicit local attempt (`pending`, `completed`, or `failed`) with its click-generated request ID and the exact research snapshot ID. It passes that ID once to the client API, renders it compactly while generating/failing, and replaces it only on a new click. Current mutation state wins over query refetch; a matching persisted completion restores Completed after refresh; persisted failed jobs are neutral compact history only.
- `generateAiDraft` now exports `AiDraftGenerationError` and validates only allowlisted public safe codes from the known `{ ok:false, code, message, request_id, retryable }` envelope. With installed `@supabase/supabase-js`/`functions-js` 2.97, it reads a non-2xx body only from `FunctionsHttpError.context.clone().json()`. Unavailable/unstructured responses become `request_failed` with the caller-generated ID. UI deliberately shows neither the safe message nor any internal/provider detail.
- Added campaign API and card tests for historical failure, confirmed button availability, one-ID pending state/duplicate prevention, pre-ledger failure, validation redaction, matching persisted completion, successful mutation over historical failure, pending-query race, explicit subsequent ID/no retry, and retained narrow/dark utility classes. Required verification passed: `npm run verify:migrations` (18 migrations), `npm run typecheck`, `npm run lint`, `npm run test:unit` (110 tests), `npm run build`, and `git diff --check`.
- Browser-control tooling was unavailable in this environment, so the authenticated light/dark/narrow browser smoke could not run. No migration, Edge Function, secret, provider call, deployment, or PR merge occurred. Frontend restart/redeployment and one authenticated supervised production generation remain pending.

## OUTREACH-03C-QUALITY-04 commercial draft quality guardrails

- **Task goal:** Improve first-touch commercial draft quality without changing the supervised architecture: semantically recognize one legitimate low-pressure CTA, enforce consistent Spanish register, preserve concept/reference proof status, require concise grounded copy, and version the material prompt contract as `outreach_draft_v2` while retaining schema `draft_v1`.
- **Current-state evidence:** The existing branch is clean at `152d869` and matches `origin/codex/outreach-03c-ai-draft`; draft PR #21 remains open and unmerged. The first successful authenticated Hotel Marina Badalona generation completed with Research Version 2 and immutable Message Version 1 on `gpt-5.4-mini` (1,011 total tokens, 3,788 ms, Edge Function HTTP 200). It persisted one AI draft and moved the member only to `draft_ready`, with no automatic review, approval, Gmail draft, send, retry, or campaign progression. Commercial review rejected that message because semantic CTA recognition missed its existing `¿Os encajaría...?` question and appended a duplicate formal CTA, mixed Spanish registers, allowed ambiguous concept/client wording for Oria House Barcelona, and produced excessive first-touch length. Message Version 1 and Research Versions 1/2 remain immutable production history.
- **Files inspected:** `AGENTS.md`; git/PR/migration state; `supabase/functions/_shared/ai-draft.ts` and its focused tests; `supabase/functions/outreach-ai-runtime/index.ts`; `20260801000007_supervised_ai_draft_generation.sql`; `scripts/verify-migrations.mjs`; `tasks/OUTREACH-03C.md`; `docs/current-state.md`; and ADR 0005.
- **File plan:** Update only the shared draft prompt/validation/completion helper and focused tests, the runtime start-version argument if required, one next monotonic additive migration, migration verification, generated database types only if the RPC shape changes, and the three requested documentation records. No browser UI surface, historical migration, existing database row, message, or research snapshot will be edited.
- **Implementation plan:** Bump new jobs to `outreach_draft_v2`; replace only `start_ai_draft_job` additively so V1 and V2 are accepted and both map to `draft_v1`; expand multilingual semantic CTA recognition and count all CTA/request questions so exactly one valid CTA is unchanged, zero alone may receive one register-compatible deterministic fallback, and multiple/aggressive requests reject; validate Spain/vosotros versus explicit formal plural mixing without treating neutral `su` alone as formal; detect proof status terms in persisted context and reject wording that turns concept/reference material into implied client work; enforce a 180-word body ceiling without truncation; and strengthen trusted brevity, subject, proof, register, and signature instructions.
- **Risks:** Semantic patterns can create false positives or weaken an existing safety check; word counting must work across supported languages; proof-status validation must reject only unsupported relationship implications; function replacement must preserve service-role-only access, organization checks, idempotency, RLS boundaries, and historical compatibility. Rollback/forward-fix is a later additive function replacement selecting V1 only if V2 must be disabled; no destructive rollback or historical mutation is permitted.
- **Acceptance criteria:** Approximately 100–150-word first outreach with at most 180 words; two to four strongest researched details; concise non-clickbait subject; one observation, one offer/optional verified proof, exactly one low-pressure CTA; consistent resolved-language/register; explicit concept/reference fidelity; no provider retry or deterministic substantive rewrite; unchanged immutable versioning and `draft_ready`-only lifecycle; all existing factuality, identity, format, pressure, approval, Gmail, and no-send safeguards preserved.
- **Tests:** Add focused cases for semantic Spanish CTA variants, English/Ukrainian/Russian equivalence, duplicate/aggressive CTA rejection, missing-only deterministic completion with compatible Spanish register, Spain-register consistency and neutral `su`, concept/reference proof fidelity, 100–150-word success, over-180-word rejection/no truncation, supported-language success, no second provider request, rejected-result no-side-effects, and immutable next-version behavior. Run the focused AI-draft suite plus migration verification, typecheck, lint, full unit tests, build, and `git diff --check`.
- **Manual smoke and production acceptance:** No OpenAI call, migration apply, deployment, send, or production mutation during implementation. After reviewed migration apply, Edge Function redeployment, and frontend restart/redeploy only if relevant, run exactly one authenticated production generation against Research Version 2. It should create Message Version 2, leave Version 1 unchanged, use consistent Spain Spanish, stay within 180 words (target 100–150), retain reviewed research specificity, represent Oria House explicitly as a concept/reference, and contain exactly one CTA with no automatic review, approval, Gmail draft, send, or campaign progression. No UI changed here, so implementation-time light/dark smoke is not applicable.
- **Proposed commit:** `fix(outreach): improve AI draft commercial quality`
- **Documentation impact:** Update this record, `docs/current-state.md`, and ADR 0005 with the successful technical production evidence, commercial rejection, immutable Version 1 status, V2 prompt/validation contract, additive migration/deployment requirements, and pending single Version 2 acceptance retest.

### QUALITY-04 implementation and verification record

- New draft jobs now use `outreach_draft_v2` with the unchanged strict `draft_v1` JSON shape. The single additive `20260801000008_accept_ai_draft_prompt_v2.sql` replaces only `start_ai_draft_job`, accepts historical V1 and current V2, maps both to `draft_v1`, rejects unknown versions, retains service-role-only execution and organization/member/latest-research checks, and contains no historical ledger, message, activity, or member mutation. The RPC signature is unchanged, so generated database types require no diff.
- Trusted instructions now define a 100–150-word first outreach, a hard 180-word limit, two to four strongest details, concise subject rules, no signature, exactly one final CTA, consistent Spanish register with Spain preference, and exact proof-status fidelity. Validation semantically recognizes supported-language CTA structures, rejects multiple/aggressive requests, rejects explicit Spanish-register mixing without treating `su` alone as formal, rejects over-180-word bodies without truncation, and rejects a named concept/reference whose status disappears or becomes unsupported client work. Deterministic completion still runs only after the exact missing-CTA reason and chooses a vosotros-compatible Spanish fallback when the body clearly uses that register.
- Focused tests cover the production `¿Os encajaría...?` CTA unchanged, Spanish semantic variants, English/Ukrainian/Russian success, duplicate/aggressive requests, register consistency, neutral `su`, proof concept pass/client implication/status removal failures, 100–150-word success, over-180-word rejection/no truncation, missing-only completion, one provider request, failed-result no-side-effects, append-only immutable success, and Version 1 → Version 2 behavior. Final checks passed: `npm run verify:migrations` (19 migrations), `npm run typecheck`, `npm run lint`, `npm run test:unit` (129 tests), `npm run build`, and `git diff --check`. The existing Vite large-chunk advisory remains unchanged.
- `npx supabase migration list` was attempted read-only, but linked authentication failed with `SQLSTATE 28P01`; migration apply state therefore remains a reviewed release check. No OpenAI call, migration apply, Edge Function/frontend deployment, production write, message/research edit, review submission, Gmail action, send, or PR merge occurred. No UI changed, so implementation-time light/dark smoke is not applicable. Reviewed migration apply, Edge Function redeployment, and exactly one authenticated Research Version 2 → Message Version 2 production acceptance retest remain pending; Message Version 1 must remain unchanged and unreviewed.

## OUTREACH-03C-QUALITY-05 required proof and latest metadata correlation

- **Task goal:** Version the draft contract as `outreach_draft_v3` so a reviewed recommended case that is verifiably identified in non-empty proof context must appear in the provider draft with faithful concept/reference status, and correlate completed AI-card metadata to the latest persisted message generation rather than research identity alone.
- **Current-state evidence:** The branch is clean at `6c7ee3c` on `codex/outreach-03c-ai-draft`; PR #21 remains draft and unmerged. Production Message Version 2 completed correctly from Research Version 2 with job `fb3d1ebb-d0f6-4747-b9ac-254a897b17a9`, `outreach_draft_v2` / `draft_v1`, 1,184 total tokens, and 3,261 ms. It is an immutable AI draft and left the member at `draft_ready`, but omitted the verified Oria House Barcelona concept/reference. The AI card could also retain Version 1 metadata because a local completed result matched only the unchanged Research Version 2 and therefore outranked the newer persisted Version 2 generation.
- **Files inspected:** `AGENTS.md`; this task record and current-state/ADR documentation; shared AI draft and research helpers/tests; Edge Function draft flow and contract test; migrations `20260801000007` and `20260801000008`; migration verifier; campaign API/types; AI Draft card and tests; Campaign Workspace query flow.
- **File plan:** Update the shared proof-resolution/draft prompt and validator plus focused tests; pass the reviewed recommended case through the existing runtime validator; add only additive migration `20260801000009_accept_ai_draft_prompt_v3.sql`; extend migration/runtime guards; minimally correct AI-card metadata selection and its regression tests; update only this task, current-state, and ADR 0005. The RPC signature and generated database types remain unchanged.
- **Implementation plan:** Reuse one verified-proof resolver based on existing proof-context identifier extraction. Serialize a required proof reference only when both inputs are present and match; instruct the provider to mention it once; reject omission without deterministic proof insertion or retry; retain proof-status/client-relationship validation. Advance only the prompt provenance to V3 while keeping `draft_v1`. For UI state, allow a local completed result to win only while it is not older than, or is directly correlated with, the latest persisted message generation; a newer persisted message selects its linked latest job metadata.
- **Risks and safeguards:** Normalized title matching could force an unverified case, proof enforcement could weaken length/CTA checks, or UI correlation could hide an in-flight success before workspace refresh. Matching therefore uses the existing reviewed proof-title extractor, omission is a normal safe validation failure, all existing full validation still runs, and current pending/failure authority remains unchanged. Migration `00009` replaces only the service-role start RPC, preserves V1/V2/V3 history, retains organization membership and latest-research checks, and changes no RLS or lifecycle behavior. Rollback/forward-fix is a later additive start-function replacement that stops accepting V3; applied migrations and historical ledger/messages are never rewritten.
- **Acceptance criteria:** V3/draft_v1 provenance; required verified recommended proof is present and status-faithful; unmatched or absent recommended proof remains optional and never invented; no deterministic proof copy or provider retry; all QUALITY-04 language/register/CTA/length/claim boundaries remain; persisted V1 → V2 on Research V2 displays V2-linked metadata; immutable messages and `draft_ready`-only lifecycle remain unchanged.
- **Tests:** Add required-proof present/omitted/client-claim/empty-recommendation/unverified-recommendation cases, V3/schema assertions, migration guards, and the real same-research Job A → Job B metadata regression while retaining current attempt failure, historical failure, success, and initial-state coverage. Run migration verification, typecheck, lint, full unit tests, build, and `git diff --check`.
- **Manual smoke:** No provider call or production operation in implementation. UI classes/layout do not change, so automated state regression plus existing light/dark class coverage are applicable here; authenticated production V3 smoke remains a later explicit task after migration apply, Edge Function deploy, and hard refresh.
- **Proposed commit:** `fix(outreach): require verified proof and sync draft metadata`
- **Documentation impact:** Record Message V2 runtime/provenance success, proof omission and stale metadata findings, V3 contract/migration, immutable V1/V2 status, and pending single V3 production acceptance. Do not change the global roadmap.

### QUALITY-05 implementation record

- Shared proof resolution now returns the verified proof-context identifier behind a reviewed recommended case and is reused by research matching, serialized draft input, trusted V3 instructions, and the draft validator. New jobs use `outreach_draft_v3` with unchanged `draft_v1`; a verified required reference omitted from the body fails as `body_missing_required_proof_reference` before CTA completion, while empty or unmatched recommendations remain optional. Existing proof-status/client-relationship, language/register, length, CTA, factuality, identity, no-retry, and no-send boundaries remain intact.
- Added forward-only `20260801000009_accept_ai_draft_prompt_v3.sql`. It replaces only `start_ai_draft_job`, accepts immutable historical V1/V2 plus current V3, maps all to `draft_v1`, retains service-role-only access and organization/latest-research checks, and changes no historical row, RLS policy, completion RPC, signature, or generated database type. A later additive start-function replacement is the documented forward-fix/rollback path.
- AI Draft metadata now treats a completed local result as current only when no message has persisted yet, it matches the latest message's generation ID, or it is a genuinely newer provisional message version. A newer persisted Job B therefore wins over stale Job A even when both use Research Version 2. Current pending/failure authority and neutral historical failures are unchanged.
- Focused coverage adds verified proof present/omitted/false-client/empty/unmatched cases, V3/schema/migration guards, resolver reuse, and the production same-research Version 1/Job A → Version 2/Job B metadata sequence. Message Versions 1 and 2 remain immutable production evidence. Migration apply, Edge Function deploy, hard refresh, exactly one Message Version 3 smoke, and final DB/audit/activity inspection remain pending; no provider or production operation occurs in this task.
